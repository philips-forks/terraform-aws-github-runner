name: "Terraform lifecycle"

on:
  pull_request:
    paths: ["**/*.tf", "**/*.hcl", ".github/workflows/ministack.yml"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  AWS_ACCESS_KEY_ID: "000000000000"
  AWS_DEFAULT_REGION: eu-west-1
  AWS_EC2_METADATA_DISABLED: "true"
  AWS_ENDPOINT_URL: http://localhost:4566
  AWS_REGION: eu-west-1
  AWS_SECRET_ACCESS_KEY: test-only
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"

jobs:
  terraform_lifecycle:
    name: Apply/destroy ${{ matrix.example }} against MiniStack (${{ matrix.iac.name }} ${{ matrix.iac.version }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        iac:
          - name: terraform-1.5.6
            version: "1.5.6"
            binary: terraform
            lockfile: .terraform.lock.hcl
          - name: terraform-latest
            version: latest
            binary: terraform
            lockfile: .terraform.lock.hcl
          - name: tofu-1.11
            version: "1.11"
            binary: tofu
            lockfile: .terraform.lock.hcl.tofu
          - name: tofu-latest
            version: latest
            binary: tofu
            lockfile: .terraform.lock.hcl.tofu
        example:
          - base
          - prebuilt
          - default
          - ephemeral
          - multi-runner
          - multi-runner-v2
          - migration-test
          - termination-watcher
    services:
      ministack:
        image: ghcr.io/ministackorg/ministack:1.5.13@sha256:ce3c906f2866ff953ce4c56f06b1fa3e453bc32e41c00de17b5f5a8672c5a42c
        ports:
          - 4566:4566
        env:
          MINISTACK_ACCOUNT_ID: "000000000000"
          MINISTACK_REGION: eu-west-1
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Terraform
        if: matrix.iac.binary == 'terraform'
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: ${{ matrix.iac.version }}
          terraform_wrapper: false

      - name: Setup OpenTofu
        if: matrix.iac.binary == 'tofu'
        uses: opentofu/setup-opentofu@a1320f892987e89d278cc92dc5adc984fb93aca4 # v2.0.2
        with:
          tofu_version: ${{ matrix.iac.version }}
          tofu_wrapper: false

      - name: Mark repository as safe
        shell: sh
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - id: init
        name: ${{ matrix.iac.name }} init
        env:
          EXAMPLE: ${{ matrix.example }}
          IAC_BINARY: ${{ matrix.iac.binary }}
          IAC_LOCK_FILE: ${{ matrix.iac.lockfile }}
        run: tests/ministack/run-example.sh init "$EXAMPLE"

      - id: apply
        name: ${{ matrix.iac.name }} apply
        env:
          EXAMPLE: ${{ matrix.example }}
          IAC_BINARY: ${{ matrix.iac.binary }}
          IAC_LOCK_FILE: ${{ matrix.iac.lockfile }}
        run: tests/ministack/run-example.sh apply "$EXAMPLE"

      - name: ${{ matrix.iac.name }} destroy
        if: always() && steps.init.outcome == 'success'
        env:
          EXAMPLE: ${{ matrix.example }}
          IAC_BINARY: ${{ matrix.iac.binary }}
          IAC_LOCK_FILE: ${{ matrix.iac.lockfile }}
        run: tests/ministack/run-example.sh destroy "$EXAMPLE"
